json.extract! test, :id, :name, :points, :type, :target, :include, :position, :show_output, :skip, :timeout, :visibility, :assignment_id, :created_at, :updated_at
json.url test_url(test, format: :json)
